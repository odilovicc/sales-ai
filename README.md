# sales‑ai — Telegram Lead Bot + AI‑фильтрация лидов

## 📌 Что это  

sales‑ai — бот‑userbot для Telegram, который:

- слушает указанные Telegram‑каналы как реальный userbot (через GramJS),  
- анализирует новые сообщения через локальную LLM (Ollama + Llama 3.2),  
- фильтрует “лидов” (компании + контакт) по заданным категориям,  
- сохраняет подходящие лиды в `leads.xlsx`,  
- при желании синхронизирует их с Google Sheets (если настроен),  
- умеет обрабатывать текст + подписи к фото/медиа, делает кликабельные ссылки на исходные сообщения, проверяет дубликаты (по телефону) и даёт гибкие настройки через `.env`.

Проект рассчитан на приватность (всё работает локально), автономность и гибкость в конфигурации.

---

## ✅ Возможности  

- Userbot‑подключение к Telegram через GramJS  
- Мониторинг каналов в режиме реального времени  
- Локальная LLM‑фильтрация → нет зависимости от внешних API  
- Сохранение лидов в Excel (`leads.xlsx`)  
- Опциональная синхронизация с Google Sheets  
- Поддержка фильтрации: текст + подписи к фото/медиа  
- Проверка на дубликаты (по телефону)  
- Простой и гибкий конфиг через переменные в `.env`  

---

## 🏗 Структура проекта  

/ (root)
├── index.ts # Точка входа бота
├── src/ # Основной код
│ ├── config/ # Конфигурация
│ │ └── index.ts
│ ├── services/ # Сервисы: Telegram‑клиент, LLM, экспорт, Google Sheets
│ │ ├── telegram.ts
│ │ ├── llm.ts # Интерфейс к Ollama / LLM
│ │ ├── excel.ts # Excel‑экспорт лидов
│ │ └── googleSheets.ts # Google Sheets sync (опционально)
│ ├── types/ # TS‑типы
│ │ └── index.ts
│ └── utils/ # Утилиты, логгер и пр.
│ └── logger.ts
├── .env.example # Пример конфигурации
├── google‑credentials.example.json # Шаблон для Google Sheets настроек
├── tsconfig.json # Настройки TypeScript
├── package.json # npm / bun зависимости
├── install.sh # (Опционально) скрипт установки
└── sync-google-sheets.ts # Скрипт для запуска ручной синхронизации с Google Sheets


---

## 🚀 Быстрый старт  

### 🔧  Предпосылки  

- bun (или npm / yarn — по желанию),  
- Учетная запись Telegram + `API_ID`, `API_HASH` (получить через https://my.telegram.org/apps),  
- Ollama + модель Llama 3.2, запущенная локально (или другая совместимая LLM),  
- (Опционально) Google‑аккаунт + service account для Google Sheets, если хотите синхронизацию с таблицами.

### ✅ Установка  

```bash
git clone https://github.com/odilovicc/sales‑ai.git  
cd sales‑ai  
bun install
```

### 📦 Настройка

Скопируй .env.example в .env и заполни обязательные поля:

```
API_ID=…  
API_HASH=…  
PHONE_NUMBER=+998…  
CHANNELS=@channel1,@channel2,…   # Список каналов для мониторинга  
… (другие опции: ANALYZE_HISTORY, HISTORY_LIMIT, MIN_MESSAGE_LENGTH и пр.)  
```

(Если используешь Google Sheets) Скопируй google‑credentials.example.json, получи свой service‑account JSON и укажи путь к нему.

▶️ Запуск

# Запуск бота:

```bash
bun start   # или npm run start / yarn start  
```

(Убедись, что Ollama с моделью Llama 3.2 работает: ollama serve.)
📑 Конфигурация и переменные окружения

### Пример .env‑конфигурации:

API_ID=your_api_id  
API_HASH=your_api_hash  
PHONE_NUMBER=+1234567890  
CHANNELS=@channel1,@channel2  

# Опции бота  
ANALYZE_HISTORY=false     # При запуске — анализ истории (true/false)  
HISTORY_LIMIT=50          # Сколько старых сообщений читать при старте  
MIN_MESSAGE_LENGTH=20     # Минимальная длина сообщения для анализа  
HISTORY_DELAY=1000        # Задержка между сообщениями (ms)  
CHANNEL_DELAY=2000        # Задержка между каналами (ms)  

# Google Sheets (опционально)  
GOOGLE_CREDENTIALS_PATH=./google‑credentials.json  
GOOGLE_SHEET_ID=your_sheet_id  

# 📝 Лицензия

MIT

# 🙋‍♂️ Как помочь / контрибьют

Форкай проект и делай ветвь с фичей.

Следуй стилю кода (TypeScript), пиши чисто.

Предлагай фичи, баги — открывай Issues / Pull Requests.

Не заливай .env, .session, creds в репо.

